# Project Goal

## Canonical Goal

<!-- ONE sentence. This exact line is hashed by memory/goal_guard.py and the hash
     is recorded in 21-agent-roster.md, so wave-boundary drift checks can tell
     whether the goal changed without a logged decision. Do not delete this
     heading: without it goal_guard refuses to run (exit 2, UNANCHORED). -->

Replace this line with the one-sentence canonical goal.

## Summary

Describe the project in plain English.

## Success Criteria

- 

## Execution Level

<!-- The CEO records the chosen tier here and, once the user picks it, sets
     `Locked: yes`. `memory/validate_run.py` reads the `Locked:` field, so a run
     cannot close while it still says `no` — that is intentional (a scaffold is
     not a finished run). -->

```text
Recommended level: Solo Agent Loop / Mini Swarm / Full Swarm
Reason:
User override:
Tier:
Locked: no
```

## Definition of Done

<!-- Replace the placeholder below with the real, checkable done conditions.
     `memory/validate_run.py` requires this heading, at least one `- [ ]` item,
     and NO remaining `TBD`, so a run cannot close on this placeholder — fill it
     in (audit 2026-07-25: the template used to omit this section entirely, so
     every scaffolded run failed the DoD check with no way to see why). -->

- [ ] TBD — replace with a concrete, verifiable done condition

