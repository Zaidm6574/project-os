# Project Goal

## Canonical Goal

<!-- ONE sentence. This exact line is hashed by memory/goal_guard.py and the hash
     is recorded in 21-agent-roster.md, so wave-boundary drift checks can tell
     whether the goal changed without a logged decision. Do not delete this
     heading: without it goal_guard refuses to run (exit 2, UNANCHORED). -->

Replace this line with the one-sentence canonical goal.

## Summary

Describe the project in plain English.

## Success Criteria

- 

## Execution Level

```text
Recommended level: Solo Agent Loop / Mini Swarm / Full Swarm
Reason:
User override:
```

